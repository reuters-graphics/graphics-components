import { Meta, Canvas } from '@storybook/blocks';

import * as ScrollerPathStories from './ScrollerPath.stories.svelte';

<Meta of={ScrollerPathStories} />

# ScrollerPath

The `ScrollerPath` component TKTKTKTK

```svelte
<script>
  import { ScrollerPath } from '@reuters-graphics/graphics-components';
</script>

<!-- TKTKTK -->
<ScrollerPath />
```
