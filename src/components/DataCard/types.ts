/**
 * Named card widths. Sets a card's `max-width`; override either with the
 * `--dc-card-max-width` CSS variable.
 * - `"single"` — one-number cards (~115px).
 * - `"double"` — two-column cards and charts (~230px).
 */
export type DataCardWidth = 'single' | 'double';
