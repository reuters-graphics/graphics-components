---
'@reuters-graphics/graphics-components': patch
---

Increase the exaggeration used in the TileMap "Enabling 3D terrain" example so the relief is actually visible at a whole-of-Spain zoom, and guard `setTerrain` in the doc snippet against a missing terrain source.
