---
'@reuters-graphics/graphics-components': minor
---

Add an optional `legend` snippet to `TileMap` so a legend and map can share a single `GraphicBlock`, with one title and the standard block spacing, instead of being stacked as separate blocks.
